# Руководство по использованию Telegram бота

## Описание

Telegram бот для работы с CRM системой. Позволяет просматривать компании, контакты и сделки прямо из Telegram.

## Функционал

- 🔐 **Авторизация** через email и пароль
- 🏢 **Просмотр компаний** с пагинацией и детальной информацией
- 👤 **Просмотр контактов** с пагинацией и детальной информацией
- 💼 **Просмотр сделок** с возможностью изменения статуса
- 📱 **Удобное меню** с кнопками для навигации

## Установка и настройка

### Вариант 1: Бэкенд через Docker, бот локально (рекомендуется)

#### 1. Запуск бэкенда

В первом терминале запусти Docker контейнеры:

```bash
docker-compose up -d
```

Проверь, что API доступен: открой `http://localhost:8000/api/v1/docs` в браузере.

#### 2. Установка зависимостей для бота

Во втором терминале:

```bash
# Активируй виртуальное окружение (если используешь)
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установи зависимости
pip install -r requirements.txt
```

#### 3. Настройка переменных окружения

Добавь в файл `.env` (в корне проекта):

```env
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
API_BASE_URL=http://localhost:8000/api/v1
```

**Важно:** `API_BASE_URL` должен указывать на адрес, где запущен бэкенд. Если бэкенд в Docker, используй `http://localhost:8000/api/v1`.

#### 4. Запуск бота

В том же втором терминале:

```bash
python run_bot.py
```

Или через модуль:

```bash
python -m app.bot.main
```

### Вариант 2: Все локально (без Docker)

Если запускаешь бэкенд локально через `uvicorn app.main:app --reload`, порядок тот же, но Docker не нужен.

### Вариант 3: Бот в Docker (опционально)

Можно добавить бота в `docker-compose.yml` как отдельный сервис, но для разработки удобнее запускать локально.

## Использование

### Авторизация

1. Найди бота в Telegram и отправь команду `/start`
2. Введи свой email (который используется для входа в CRM)
3. Введи пароль
4. После успешной авторизации появится главное меню

### Главное меню

После авторизации доступны следующие кнопки:

- **🏢 Компании** - просмотр списка компаний
- **👤 Контакты** - просмотр списка контактов
- **💼 Сделки** - просмотр списка сделок
- **🔐 Выйти** - выход из системы

### Работа с компаниями

1. Нажми кнопку **🏢 Компании**
2. Отобразится список компаний с пагинацией
3. Нажми на компанию для просмотра детальной информации
4. Используй кнопки навигации для перехода между страницами

### Работа с контактами

1. Нажми кнопку **👤 Контакты**
2. Отобразится список контактов с пагинацией
3. Нажми на контакт для просмотра детальной информации
4. Используй кнопки навигации для перехода между страницами

### Работа со сделками

1. Нажми кнопку **💼 Сделки**
2. Отобразится список сделок с пагинацией
3. Нажми на сделку для просмотра детальной информации
4. В детальном просмотре можно изменить статус сделки:
   - 🆕 Новая
   - ⚙️ В работе
   - ✅ Завершена
   - ❌ Отменена

### Выход из системы

Нажми кнопку **🔐 Выйти** или отправь команду `/logout` для выхода из системы.

## Структура проекта

```
app/bot/
├── __init__.py          # Инициализация модуля
├── config.py            # Конфигурация бота
├── main.py              # Главный файл бота
├── api_client.py        # Клиент для работы с API
├── keyboards.py         # Клавиатуры для бота
└── handlers/            # Обработчики
    ├── __init__.py
    ├── auth.py          # Авторизация
    ├── companies.py     # Компании
    ├── contacts.py      # Контакты
    └── deals.py         # Сделки
```

## Требования

- Python 3.11+
- Запущенный API сервер (FastAPI)
- Telegram Bot Token от @BotFather

## Команды бота

- `/start` - Начать работу с ботом (авторизация)
- `/logout` - Выйти из системы
- `/cancel` - Отменить текущую операцию (во время авторизации)

## Примечания

- Токен авторизации сохраняется в памяти бота и теряется при перезапуске
- Для работы бота необходимо, чтобы API сервер был запущен и доступен
- Все данные берутся из API, бот не хранит данные локально
- Бот работает только с данными текущего авторизованного пользователя

## Устранение неполадок

### Бот не отвечает

1. Проверь, что бот запущен (`python run_bot.py`)
2. Проверь правильность токена в `.env`
3. Проверь логи на наличие ошибок

### Ошибка авторизации

1. Убедись, что API сервер запущен
2. Проверь правильность `API_BASE_URL` в `.env`
3. Проверь, что пользователь существует в системе

### Ошибка при получении данных

1. Проверь, что API сервер доступен
2. Проверь, что токен авторизации не истек
3. Попробуй выйти и войти заново (`/logout` → `/start`)

